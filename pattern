#include<conio.h>
#include<stdio.h>
void main()
{
int i,n,j;
printf("enter the n<=%d");
scanf("%d",&n);
for(i=0;i<n;i++)
{
  if((i+j)<(n-1))
  {
    printf(" ");
  }
  else
  {
    printf("#");
  }
}
getch();
  
