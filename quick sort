#define max 5
#include<stdio.h>
#include<conio.h>
void msin()
{
int a[max],i,j,n,temp;
printf("enter trhe limit ");
scanf("%d",&n);
for(i=0;i<n;i++)
{ 
  scanf("%d",&n);
}
for(i=0;i<n;i++)
{
  for(j=1;j<n-i;j++)
    {
      if(a[i]>a[j])
      
      {
        temp=a[i];
        a[i]=a[j];
        a[j]=temp;
      }
    }
}    
for(i=0;i<n;i++)
{
  printf("%d",sorted array);
}
getch();
